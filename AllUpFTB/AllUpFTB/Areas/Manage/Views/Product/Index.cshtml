@model List<Product>
@{
    int counter = 0;
}

<div class="container-fluid">
    <div class="row justify-content-between align-items-center">
        <h1>Product</h1>
        <a class="btn btn-primary" asp-controller="Product" asp-action="create">Create</a>
    </div>
    <table class="table contact-form">
        <thead>
            <tr>
                <th scope="col">No</th>
                <th scope="col">Name</th>
                <th scope="col">Description</th>
               
                <th scope="col">CostPrice</th>
                <th scope="col">SalePrice</th>
                <th scope="col">DiscountPercent</th>
                <th scope="col">IsAvailable</th>
                <th scope="col">Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var products in Model)
            {
                counter++;
                <tr>
                    <th scope="row">@counter</th>
                    <td>@products.Name</td>
                    <td>@products.Description</td>
                   
                    <td>@products.CostPrice</td>
                    <td>@products.SalePrice</td>
                    <td>@products.DiscountPercent</td>
                    <td>@products.IsAvailable</td>
                    <td>
                        <a asp-controller="Product" asp-action="update" asp-route-id="@products.Id" class="btn btn-success">Update</a>
                        <a asp-controller="Product" asp-action="delete" asp-route-id="@products.Id" class="btn btn-danger">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>